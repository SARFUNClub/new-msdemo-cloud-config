gateway:
  server:
    port: 8765
  config:
    url:
      eureka: http://discovery-service:8761/eureka/
      zipkin: http://zipkin-service:8762
      
zuul:
  host:
    connect-timeout-millis: 160000
    socket-timeout-millis: 160000
  prefix: /api
  
  routes:    
    leave-apply-service:
      path: /**
      serviceId: leave-apply-service
      strip-prefix: false
      ribbon:
        ReadTimeout: 100000
